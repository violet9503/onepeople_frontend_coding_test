import Input from "../../../../../ui/Input/Input";

const Long = () => {
  return (
    <Input
      aria-label="short answer"
      disabled
      placeholder="1000자 이내로 답변을 받을 수 있는 란입니다."
    />
  );
};

export default Long;
